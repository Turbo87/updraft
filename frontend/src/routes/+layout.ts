// Render as a client-side single-page app: no server-side rendering, no
// prerendering. The static adapter's index.html fallback serves every route.
export const ssr = false;
export const prerender = false;
